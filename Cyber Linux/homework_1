home work Day 1:
في الشرح كنت استخدم توزيعه تسمي xubuntu
1 what is the Desktop Interface used in Xubuntu ?
Xfce
2 What command clears the contents of your terminal display?
clear
3 what is the latest Linux kernel version Number? 
4.18.0-16-generic (that's what I have).

4 create a Folder"folder1", go to it and create a file"New File ", and then Copy the File to the Desktop. ( write every command ).
  cd Desktop
  mkdir folder1
  cd folder1
  touch NewFile.txt
  
  
